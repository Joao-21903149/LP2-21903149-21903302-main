package pt.ulusofona.lp2.deisiGreatGame;
import org.junit.Assert;
import org.junit.Test;

import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.Arrays;


public class TestGameManager {
    /*
    GameManager gm = new GameManager();
    String[][] board = {{"0","Pedro","Java","Purple"},{"1","Mariza","Kotlin","Green"},{"2","Joao","C","Brown"}};
    String[][] tools = {{"0","1","2"},{"1","2","3"}};

    @Test
    public void testGameManagerNormal(){
        gm = new GameManager();
        gm.createInitialBoard(board,49);
        gm.moveCurrentPlayer(3);
    }

    @Test
    public void testGameManagerWithTools(){
        gm = new GameManager();
        gm.createInitialBoard(board,49,tools);
        gm.moveCurrentPlayer(1);
        Assert.assertEquals(gm.getCurrentPlayerByID(0).toString(), "0 | Pedro | 2 | No tools | Java | Em Jogo");
        gm.reactToAbyssOrTool();
        gm.moveCurrentPlayer(2);
        gm.reactToAbyssOrTool();
        Assert.assertEquals(gm.getCurrentPlayerByID(1).toString(), "1 | Mariza | 3 | Testes unitários | Kotlin | Em Jogo");
        Assert.assertEquals(gm.getProgrammersInfo(),"Pedro : No tools | Mariza : Testes unitários | Joao : No tools");
        Assert.assertEquals(gm.getTitle(2),"Erro de lógica");
        Assert.assertEquals(gm.getImagePng(3),"playerGreen.png");
    }

    @Test
    public void testAbismo(){
        Abismo a = new Abismo(1, "omega");
        Assert.assertEquals(a.toString(),"Abismo{id=1, titulo='omega', pos=0}");
    }

     */


}
